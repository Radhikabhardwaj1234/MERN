//window object represents the whole tab of the browser where the diffrent properties like localStorage , cookies , etc exist
//Document object is actually a property or a subset of window object which represents the web page where all the elements that we write will be displayed .

//If we mold the html code in to ome object that object will be document object , we can acees anything on the web page with the help of document object.

//DOM - above modeling of a web page into object is called document object model
//every html tag will also converted in to a node of dom tree.